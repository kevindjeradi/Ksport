import 'package:flutter/material.dart';

class RoutineCreatorPage extends StatelessWidget {
  const RoutineCreatorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
